n = int(input("Enter a number for validation: "))
def prime(val):
  v = bool(0)

  for i in range(2,n):
    if(n % 2 == 0):
      v = bool(1)
      break
  return v

if(prime(n) == False):
  res = "Prime"
  print(res)
else:
  res = "Not Prime"
  print(res)
